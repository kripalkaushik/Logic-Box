# Pattern Generator and Number Analyzer

This README describes how the Python program works.

## 📌 Overview

This Python script provides two main functionalities: 1. **Generate a
pattern** of stars based on user input. 2. **Analyze a range of
numbers** to find even, odd numbers, total count, and sum.

------------------------------------------------------------------------

## 🧾 Program Code

``` python
print("Welcome to the Pattern Generator and Number Analyzer! ")

while True:
    print("Select an option : ")
    print("1.Genrate a Pattern")
    print("2.Analyze a Range of Number")
    print("Exit")

    choice = int(input("Enter your choice: "))

    if choice == 1:
        Num = int(input("enter the number of rows for the patterns : "))
        print("patterns")
        for i in range(1,Num+1):
            print("*" * i)

    elif choice ==2:
        start = int(input("Enter your range: "))
        end = int(input("Enter your range: "))

        even =[]
        odd = []
        total_sum = 0
        count = 0

        for num in range(start,end+1):
            if num %2== 0:
                even.append(num)
            else:
                odd.append(num)

            total_sum += num
            count += 1
        print("\n even number:",even)
        print("odd number:",odd)
        print("total Number:",count)
        print("sum:",total_sum)

    elif choice == 3:
        print("Exiting the program..")
        break

    else:
        print("invaid choice please try again and keep work hard..")
```

------------------------------------------------------------------------

## 📝 Features

### ⭐ 1. Pattern Generation

-   User enters the number of rows.
-   The program prints a star (`*`) pattern.

### 🔢 2. Number Range Analyzer

-   User enters a start and end range.
-   Program outputs:
    -   List of **even numbers**
    -   List of **odd numbers**
    -   **Total count** of numbers
    -   **Sum** of all numbers

### 🚪 3. Exit

Stops the program.

------------------------------------------------------------------------

## ✔️ How to Run This Program

1.  Copy the code into a file named `pattern_analyzer.py`
2.  Open terminal/command prompt
3.  Run:

```{=html}
<!-- -->
```
    python pattern_analyzer.py

------------------------------------------------------------------------

## 📄 Author

Generated by ChatGPT on request.
